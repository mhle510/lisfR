% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_nc_matrix.R
\name{.process_nc_matrix}
\alias{.process_nc_matrix}
\title{(Internal) Process a Single NetCDF Layer into a SpatRaster}
\usage{
.process_nc_matrix(
  layer_data,
  var_name,
  nc_type,
  ext,
  missing_value,
  i = NULL,
  j = NULL,
  dates_num = NULL,
  ext_utm = NULL,
  crs_utm = NULL,
  reproject_wgs = TRUE
)
}
\arguments{
\item{layer_data}{A matrix (or array slice) extracted via \code{ncdf4::ncvar_get()}.}

\item{var_name}{Character. Base variable name, used for layer naming.}

\item{nc_type}{Character. Dataset type. See \code{read_nc_spatial_raster()} for the
full list of supported types.}

\item{ext}{A \code{terra::ext()} object defining the raster's spatial extent.}

\item{missing_value}{Numeric. Value to convert to \code{NA}.}

\item{i}{Optional integer or character. Layer/time index, used in naming
(e.g., "_1", "_2", or a date string for FLDAS-ABH).}

\item{j}{Optional integer. Ensemble index, used only for \code{nc_type == "lis_ensemble"}.}

\item{dates_num}{Optional character vector. Pre-computed date strings, used only
for \code{nc_type == "modis"} naming.}

\item{ext_utm}{Optional \code{terra::ext()} object. Native UTM extent, used only for
\code{nc_type == "swot_2d"}.}

\item{crs_utm}{Optional character. UTM CRS proj string, used only for
\code{nc_type == "swot_2d"}.}

\item{reproject_wgs}{Logical. If \code{TRUE} (default), SWOT output is reprojected
to WGS84. Only used for \code{nc_type == "swot_2d"}.}
}
\value{
A \code{terra::SpatRaster} object representing the processed layer.
}
\description{
Consolidated internal dispatcher that converts a raw matrix/array slice
extracted from a NetCDF variable into a properly formatted \code{terra::SpatRaster}.
Behavior (transpose, flip, NA replacement, CRS, naming) is branched based on
\code{nc_type}. Each branch is self-contained and independent of the others,
so editing one dataset's logic will never affect another.
}
\keyword{internal}
